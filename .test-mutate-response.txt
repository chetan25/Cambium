{"mutations":[{"path":"src/App.tsx","type":"edit","blocks":[{"search":"export default function App() {\n  return (\n    <div className=\"min-h-screen flex items-center justify-center p-8 bg-gradient-to-br from-zinc-50 to-white\">\n      <div className=\"max-w-md text-center space-y-4\">\n        <div className=\"text-5xl select-none\">✨</div>\n        <h1 className=\"text-2xl font-semibold tracking-tight text-zinc-900\">\n          A blank canvas\n        </h1>\n        <p className=\"text-sm leading-6 text-zinc-500\">\n          Tell me what you'd like to build in the chat on the left,\n          <br />\n          or pick a quick-start to get going fast.\n        </p>\n      </div>\n    </div>\n  )\n}","replace":"import { useState } from 'react'\nimport { useHostState } from './hostState'\n\ninterface Note {\n  id: string\n  text: string\n}\n\nexport default function App() {\n  const [notes, setNotes] = useHostState<Note[]>('notes', [])\n  const [input, setInput] = useState('')\n\n  const addNote = () => {\n    if (input.trim()) {\n      setNotes([...notes, { id: Date.now().toString(), text: input.trim() }])\n      setInput('')\n    }\n  }\n\n  const deleteNote = (id: string) => {\n    setNotes(notes.filter(note => note.id !== id))\n  }\n\n  return (\n    <div className=\"min-h-screen p-8 bg-gradient-to-br from-zinc-50 to-white\">\n      <div className=\"max-w-2xl mx-auto space-y-6\">\n        <h1 className=\"text-3xl font-semibold tracking-tight text-zinc-900\">\n          Notes\n        </h1>\n        \n        <div className=\"flex gap-2\">\n          <input\n            type=\"text\"\n            value={input}\n            onChange={(e) => setInput(e.target.value)}\n            onKeyDown={(e) => e.key === 'Enter' && addNote()}\n            placeholder=\"Enter a note...\"\n            className=\"flex-1 px-4 py-2 border border-zinc-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-900 focus:border-transparent\"\n          />\n          <button\n            onClick={addNote}\n            className=\"px-6 py-2 bg-zinc-900 text-white rounded-lg hover:bg-zinc-800 transition-colors font-medium\"\n          >\n            Add\n          </button>\n        </div>\n\n        <div className=\"space-y-2\">\n          {notes.map(note => (\n            <div\n              key={note.id}\n              className=\"flex items-center justify-between p-4 bg-white border border-zinc-200 rounded-lg hover:shadow-sm transition-shadow\"\n            >\n              <span className=\"text-zinc-900\">{note.text}</span>\n              <button\n                onClick={() => deleteNote(note.id)}\n                className=\"text-zinc-400 hover:text-red-600 transition-colors\"\n              >\n                ✕\n              </button>\n            </div>\n          ))}\n        </div>\n\n        {notes.length === 0 && (\n          <p className=\"text-center text-zinc-400 py-8\">\n            No notes yet. Add one above!\n          </p>\n        )}\n      </div>\n    </div>\n  )\n}"}]}],"summary":"Created a notes app with text input, Add button, and persistent note list using useHostState.","hotReloadable":true}